import React, { FC } from 'react';

interface AppSettingsProps {
  classes: any,
}

export const AppSettings: FC<AppSettingsProps> = ({ classes }) => {

  return (
    <section>
      App
    </section>
  );
};

export default AppSettings;

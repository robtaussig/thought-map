import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles((theme: any) => ({
    root: {
        padding: 20,
    },
    header: {
        fontWeight: 600,
        fontSize: 30,
        textAlign: 'center',
    },
    text: {

    },
}));
